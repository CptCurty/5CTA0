function x = exponentialRV(lambda,m)

uniform_RV = rand(m,1);     %Representing the CDF values
x = -log(uniform_RV)*(1/lambda)


for i=1:m
CDF = 1-exp(-lambda*x);
x_new = 
x = [x x_new]
end

end

